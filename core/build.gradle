apply plugin: 'kotlin'

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    compile "org.jetbrains.kotlinx:kotlinx-coroutines-android:0.21"
    compile "com.github.kittinunf.result:result:$resultVersion"

    testImplementation "junit:junit:$jUnitVersion"
    testImplementation "com.nhaarman:mockito-kotlin:$kotlin_mock_version"
    testImplementation "org.jetbrains.kotlin:kotlin-reflect:$kotlin_version"
}

kotlin {
    experimental {
        coroutines "enable"
    }
}

//apply from: "$rootDir/build-support/jacoco.gradle"
